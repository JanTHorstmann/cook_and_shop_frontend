import { RecipeIngredientModel } from './recipe-ingredient.modelmodel';

describe('RecipeIngredientModel', () => {
  it('should create an instance', () => {
    expect(new RecipeIngredientModel()).toBeTruthy();
  });
});
